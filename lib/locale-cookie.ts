/** Cookie name used to persist the user's explicit language preference. */
export const LOCALE_COOKIE = 'NEXT_LOCALE'

/** How long the preference cookie lives (1 year, in seconds). */
export const LOCALE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365
